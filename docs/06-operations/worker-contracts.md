# Worker Contracts

Workers must produce artifacts, not vibes. Every role has required outputs and completion gates. The senior reviewer decides whether the package is blocked, review-ready or approval-ready.

# Security Policy

This public repository must never contain real client data or live credentials.

Run the safety scanner before every commit:

```bash
python3 scripts/public_safety_scan.py
```

Report security issues privately through the repository maintainers. Do not open public issues containing secrets, client documents or real Workspace URLs.

# Dashboard

The dashboard is the human control layer for We Law OS.

Run it locally:

```bash
npm install
NEXT_PUBLIC_DEMO_MODE=true npm run dev -- --hostname 127.0.0.1 --port 3012
```

It reads the synthetic demo data in `../data` and `../workspace/generated` by default.
